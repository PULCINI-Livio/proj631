import fcts_math_et_conversion_donnees as fctmath
import structure_donnees as sd

def get_best_att(list_att:list, codex:dict):
    pass
    
def tree_build(codex):
    best_att = get_best_att(codex:dict)

    finished = False
    while not finished and not (codex["donnee"]==[]):
        get_best_att()